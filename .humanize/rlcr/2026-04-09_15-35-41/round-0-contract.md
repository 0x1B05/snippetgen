# Round 0 Contract

- Mainline Objective: Complete `task1` by creating the minimal `snippetgen-demo` project skeleton, guarded by a failing-then-passing repository layout test.
- Target ACs: AC-4
- Blocking Side Issues In Scope: Only issues that prevent creating the skeleton, writing the layout test, or keeping the repository anchored to the in-repo RLCR plan.
- Queued Side Issues Out of Scope: Runtime implementation, snippet implementation, host-side generator code, probe work, and any end-to-end build work beyond the task1 skeleton.
- Success Criteria: A repository layout test fails before the skeleton exists, passes after the skeleton is created, the required directories and top-level files are present, and Round 0 has an updated summary plus a clean checkpoint commit.
