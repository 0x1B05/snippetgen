# Round 0 Contract

- Mainline Objective: Initialize RLCR governance artifacts inside `snippetgen-demo` so the next round can start the ELF-first PoC implementation against a stable, repo-local plan.
- Target ACs: AC-1, AC-4
- Blocking Side Issues In Scope: Only RLCR bootstrap issues that prevent the loop from anchoring on the repo-local plan, goal tracker, or clean git baseline.
- Queued Side Issues Out of Scope: Runtime implementation, snippet implementation, Python generator code, and any probe or simulation work beyond the ELF-first build path.
- Success Criteria: `goal-tracker.md` is fully initialized, `round-0-contract.md` exists with this scope, the repository remains clean after a local commit, and Round 0 has a written summary for Codex review.
